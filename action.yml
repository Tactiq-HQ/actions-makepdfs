# action.yml
name: 'Create PDF'
description: 'Creates PDF files from github markdown'
runs:
  using: 'docker'
  image: 'docker://ghcr.io/tactiq-hq/actions-makepdfs/actions-makepdfs:configurable-page-breaks'

inputs:
  markdown_dir:
    description: 'Location of markdown files in github repository'
    required: true
    default: 'doc'
  output_dir:
    description: 'Location to output PDF files to'
    required: true
    default: 'tmp'
  page_breaks_h1:
    description: 'Whether to insert page breaks before H1 headings'
    required: false
    default: 'true'

branding:
  icon: 'activity'
  color: 'green'
